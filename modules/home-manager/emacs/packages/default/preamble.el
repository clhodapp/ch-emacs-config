;;; ch-emacs-config-default --- Chris's Emacs Config -*- lexical-binding: t -*-
;; SPDX-License-Identifier: MIT
;;; Commentary:

;;; Code:

(eval-when-compile (require 'use-package))
(eval-when-compile (customize-set-variable 'evil-want-keybinding nil))
